describe("Kojac Ember Cache", function() {

/*


	if (_.isObject(value) {
		existing = cache.retrieve(key)
		if (_.isObject(existing)) {
			// update existing from value
		} else {
			// value = factory.manufacture(key,value)
			// cache.store(value)
		}
	} else {
		cache.store(value)
	}


*/



	it("should update models existing in cache from jsono");

	it("should pass jsono objects for models not in the cache to factory");

	it("should replace non-objects in cache");



//		, function() {
//		var cache = Kojac.EmberCache.create({});







	});

});